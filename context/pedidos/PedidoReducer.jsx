import {
  SELECCIONAR_CLIENTE,
  SELECCIONAR_PRODUCTO,
  CANTIDAD_PRODUCTOS,
} from "../../types/index.js";

const PedidoReducer = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default PedidoReducer;
